apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: semaphore-helm
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://semaphoreui.github.io/charts
    chart: semaphore
    targetRevision: 16.2.2
    helm:
      releaseName: semaphore
      values: |
        ingress:
          enabled: true
          className: traefik
          annotations:
            cert-manager.io/cluster-issuer: "letsencrypt-production"
            traefik.ingress.kubernetes.io/router.entrypoints: websecure
          hosts:
            - host: semaphore.starnix.net
              paths:
                - path: /
                  pathType: Prefix
          tls:
            - secretName: semaphore-tls
              hosts:
                - semaphore.starnix.net

        persistence:
          enabled: true
          existingClaim: semaphore-workdir-lh

        database:
          type: postgres
          host: semaphore-postgres
          port: 5432
          name: semaphore
          usernameFromSecret: false
          username: semaphore
          existingSecret: semaphore-secrets
          passwordKey: dbPassword
          options:
            sslmode: disable
          persistence:
            enabled: false

        secrets:
          existingSecret: semaphore-secrets

        admin:
          create: true
          existingSecret: semaphore-secrets
          usernameKey: adminUsername
          fullnameKey: adminFullname
          emailKey: adminEmail
          passwordKey: adminPassword

        oidc:
          enable: true
          providers: {}

        extraEnvVariables:
          SEMAPHORE_WEB_HOST: "https://semaphore.starnix.net"

        extraEnvSecrets:
          SEMAPHORE_OIDC_PROVIDERS:
            secret: semaphore-secrets
            key: oidcProviders

  destination:
    server: https://kubernetes.default.svc
    namespace: semaphore
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
